
  # Travel App Prototype

  This is a code bundle for Travel App Prototype. The original project is available at https://www.figma.com/design/GoAPhI1gDXS1RejATFvW3r/Travel-App-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  